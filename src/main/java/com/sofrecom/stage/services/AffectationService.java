package com.sofrecom.stage.services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.sofrecom.stage.models.Affectation;
import com.sofrecom.stage.models.Employe;
import com.sofrecom.stage.repository.AffectationRepository;
import com.sofrecom.stage.repository.IEmployeRepo;

@Service
public class AffectationService {
	
	/*
	@Autowired
	private AffectationRepository timesheetRepo;
	
	
	public boolean savetimesheet(Affectation timesheet) {

	if (timesheetRepo.save(timesheet) != null)
		return true;
	else
		return false;
}
	*/
	
}
